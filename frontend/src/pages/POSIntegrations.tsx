import React from 'react';
import POSIntegrations from '../components/POSIntegrations';

const POSIntegrationsPage: React.FC = () => {
  return <POSIntegrations />;
};

export default POSIntegrationsPage;